<h1> New game </h1>
<div class="letters_container">
    <% @letters.each do |letter| %>
     <div class="box_letter">
        <%= letter %>
     </div>
    <% end %>
</div>
<div>
<p>What is the longest word you can wright with those letters</p>
<form action = "/score" method="post">
<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<%= hidden_field_tag :available_letters, @letters %>
<input typer="text" name="word">
<input type="submit" value="Play">
</form>
</div>

<%# 
<form action="/score" method="" data-turbo="false">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<%# </form> %>

